{% import "fields.html.twig" as fields %}
{% extends 'layout.html.twig' %}

{% block body %}
    <div class='menu'>
    	{% include 'header.html.twig' %}

		{% include 'navigation.html.twig' with {'navigation': navigation} only %}
    </div>

    <div class="content">
		<a href="{{ path('table_list', {'entity':entity.id}) }}">« Back to all {{ entity.title(true) }}</a>

    	<h1>Delete {{ entity.title }}</h1>
    	
    
    	<form action="">
            
            <span class="not-implemented">
                Das hier funktioniert noch nicht.
            </span> 
            <p>
                Are you sure you want to delete this {{ entity.title }}?
            </p>

            <p>
                Deleting this {{ entity.title }} will also delete the following associated records:
            </p>

            <p>
                <button class="form-button">Delete</button>
            </p> 
            <p>
                <a href="{{ path('table_list', {'entity':entity.id}) }}">Cancel</a>
            </p> 
        </form>

    	
    </div>
{% endblock %}