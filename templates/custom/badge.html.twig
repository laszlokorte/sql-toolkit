<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>Badge - {{ registration.conf_name }} - {{ registration.first_name }} {{ registration.last_name }}</title>
	<style>
		.cover {
			page-break-after: right;
		}

		.title {
			position: absolute;
			top: 40%;
			left: 0;
			right: 0;
			text-align: center;
		}

		.badge-page {
			page-break-after: right;
		}

		.badge-page:last-child {
			page-break-after: avoid;
		}

		.badge {
			font-size: 1.8em;
			position: absolute;
			bottom: 4cm;
			text-align: center;
			top: 65%;
		}

		.badge-left {
			left: 1cm;
			right: 50%;
		}

		.badge-right {
			right: 1cm;
			left: 50%;
		}

		.qr {
			margin: 0.5cm;
			width: 5cm;
			height: 5cm;
			image-rendering: pixelated;
		}
	</style>
</head>
<body>
	<div class="badge-page">
		{% set qr = registration.id | qrcode %}

		<div class="badge badge-left">

			<div>
				<img class="qr" src="{{ qr }}" />
			</div>

			<span>{{ registration.first_name }} {{ registration.last_name }}</span>
		</div>
		<div class="badge badge-right">

			<div>
				<img class="qr" src="{{ qr }}" />
			</div>

			<span>{{ registration.first_name }} {{ registration.last_name }}</span>
		</div>
	</div>
</body>
</html>